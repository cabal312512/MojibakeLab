[package]
name = "mojibake-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Offline, bounded and explainable character encoding forensics"

[dependencies]
encoding_rs = "0.8.35"
chardetng = "0.1.17"
serde = { version = "1", features = ["derive"] }
unicode-script = "0.5"

[dev-dependencies]
serde_json = "1"
